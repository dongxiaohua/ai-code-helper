spring:
  application:
    name: ai-code-helper
  profiles:
    active: local
server:
  port: 8081
  servlet:
    context-path: /api
langchain4j:
  community:
    dashscope:
      chat-model:
        model-name: qwen-max
        api-key: ${DASH_SCOPE_API_KEY}
      embedding-model:
        model-name: text-embedding-v2
        api-key: ${DASH_SCOPE_API_KEY}
      streaming-chat-model:
        model-name: qwen-max
        api-key: ${DASH_SCOPE_API_KEY}
bigmodel:
  api-key: ${BIG_MODEL_API_KEY}